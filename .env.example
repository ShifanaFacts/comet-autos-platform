# Used by the Prisma CLI (prisma7.config.ts) for schema/migration commands
# run from the repo root, e.g. `npm run prisma:generate`.
# Copy this file to .env and fill in real values. Never commit .env.

DATABASE_URL="postgresql://user:password@localhost:5432/comet_autos_dev?schema=public"
